import {StyleSheet} from 'react-native';

export default styles = StyleSheet.create({
  item: {
    fontSize: 20,
    padding: 20,
    margin: 10,
    color: 'white',
    backgroundColor: 'pink',
  },
});
